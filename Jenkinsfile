#!/usr/bin/env groovy

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'This is another test!!!!!'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
          }
      }
    }
}
